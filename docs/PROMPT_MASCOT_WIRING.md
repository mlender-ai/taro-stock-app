> ⚠️ **[폐기 — 실행 금지·히스토리 보존]** 마스코트(`MASCOT.md`)를 전 에이전트에 배선하던 옛 프롬프트. **현재 기준이 아니다.** 마스코트 정체성은 폐기됨. 현재 정본: `docs/PRODUCT_VISION.md` (**주식시장의 틴더 v5 — 두 표면·한 엔진**). 아래 명령을 다시 실행하지 않는다.

docs/legacy/MASCOT.md를 전 에이전트가 인지하도록 아래 작업을 전부 실행해줘. 묻지 말고 진행.

## 1. CLAUDE.md — "FOMO Club" 섹션 하단에 추가

기존 FOMO Club 관련 내용 아래에 추가:

### FOMO Club 마스코트 (포모)
docs/legacy/MASCOT.md 참조. 포모는 FOMO Club의 마스코트이자 살아있는 지표다.
- 포모의 표정이 곧 FOMO Index다. 홈 화면의 주인공은 숫자가 아니라 표정 짓는 포모.
- 두 단계 감정 변화는 필수 구현: ①시장의 포모(진입 직후, FOMO Index 표현) → ②나의 포모(감정 선택 후 반응).
- 화면 설계 시 이 두 상태와 전환(애니메이션 + 멘트)을 반드시 포함한다.
- 디자인: 검은 얼굴 + 흰 눈, 얼굴 중심. 배경 검정, 감정 색은 포인트로만. 화면 밀도는 기리고처럼 비운다.
- 색 체계: FOMO=빨강 / 공포=파랑 / 후회=보라 / 탐욕=초록 / 확신=노랑.

## 2. MEMORY.md — 진행 상황 섹션의 FOMO Club 항목 아래에 추가

- 마스코트 '포모' 설계 확정 (docs/legacy/MASCOT.md). 화면 설계의 기준 문서.
  포모 = 살아있는 지표. 두 단계 감정 변화(시장의 포모 → 나의 포모)가 핵심 메커니즘.

알려진 제약 사항에 추가:
- 포모의 구체적 형태와 픽셀 수준 표정 묘사는 미확정. 검은 얼굴+흰 눈+얼굴 중심 원칙만 확정.
  세부 조형은 시각 작업 단계에서 결정하며, 그 전까지 플레이스홀더로 진행한다.

## 3. AGENTS.md — 관련 에이전트에 인지 추가

각 에이전트 설명에 한 줄씩 추가:

- rn-specialist:
  "FOMO Club 홈 화면은 포모 마스코트의 두 상태(시장의 포모 → 나의 포모)와 전환 애니메이션을 가진다. docs/legacy/MASCOT.md 기준으로 구현한다."

- content-planner:
  "포모의 감정 반응 멘트(시장 분위기 표현 + 사용자 감정 선택에 대한 반응)를 기획한다. 톤은 '함께 불안해하는 친구'. docs/legacy/MASCOT.md 참조."

- regulation-reviewer:
  "포모의 멘트도 검사 대상. 위로하되 투자 조언/단정 표현이 섞이지 않도록 확인한다."

## 4. fomo-index-analyst 에이전트 설명에 추가

"FOMO Index 다섯 구간(무관심/관망/관심/FOMO/광기)은 포모의 다섯 표정과 직결된다. 구간 경계값 변경 시 마스코트 표정 매핑도 함께 점검한다."

## 5. MASTER_PROMPT_FOMO_CLUB.md 갱신

Phase 3(홈 화면) 프롬프트에 마스코트 요구사항을 반영:
- "FOMO Index 메인" 블록을 "포모 마스코트(메인) + FOMO Index 숫자(보조)" 구조로 수정
- 홈 화면에 두 단계 상태(시장의 포모 → 나의 포모) + 전환 애니메이션 + 포모 멘트 추가
- 감정 선택 후 포모가 해당 감정 색으로 전환되며 반응하는 플로우 명시
- docs/legacy/MASCOT.md 참조 문구 추가

## 6. 커밋
git add -A && git commit -m "docs: 마스코트 포모 설계를 전 에이전트 + 마스터 프롬프트에 연결" && git push
```
