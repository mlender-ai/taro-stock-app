아래는 요청 주신 실행 지시에 따라 결과 화면에 '해석의 불꽃' 애니메이션을 추가하는 구현 내용입니다. React Native Animated API를 사용하며, 기존 프로젝트의 패턴과 컨벤션을 따랐습니다.

---

### 1. AnimatedFire.tsx 컴포넌트 생성
결과 화면에서 사용할 '해석의 불꽃' 애니메이션 컴포넌트를 추가합니다.

=== FILE: apps/tarot-mobile/components/AnimatedFire.tsx ===
```typescript
import React, { useEffect, useRef } from 'react';
import { Animated, StyleSheet, View } from 'react-native';
import { Colors, Spacing, Radius } from '../constants/theme';

const AnimatedFire: React.FC = () => {
  const flameOpacity = useRef(new Animated.Value(0)).current;
  const flameSize = useRef(new Animated.Value(1)).current;

  useEffect(() =>
