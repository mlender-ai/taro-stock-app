### 구현 상세 계획

주어진 지시에 따라, 타로 카드 결과 화면에 "카드 의미 미니 스토리텍스트"를 추가하려고 합니다. 이를 위해 데이터 타입을 수정하고, UI와 스타일링 파일을 업데이트하며 필요시 추가 앱 로직을 구현하겠습니다.

---

### 변경 파일 및 내용

#### 1. 카드를 설명하는 데이터를 위한 타입 추가 및 수정

=== FILE: packages/shared/types/CardTypes.ts ===
```diff
export interface TarotCard {
  id: string;
  name: string;
  description: string;
  image: string;
  // 추가: 간략한 스토리 텍스트를 위한 필드
+ miniStory: string;
}
```

---

#### 2. 타로 데이터 샘플에 `miniStory` 속성 추가

=== FILE: packages/tarot-core/data/tarotCards.ts ===
```diff
export const tarotCards: TarotCard[] = [
 
